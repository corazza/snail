v = 10;
i = 0;

def square(x)
    return x*x;
enddef

def double(x)
    return x+x;
enddef

def f(x)
    if x < 5 then
        return double(x) + square(x);
    else
        return double(x);
    endif
enddef

print f(5);
