def print(x: A) -> unit as
    __print x;
    return UNIT;
enddef

def println(x: A) -> unit as
    __print x;
    __print __newline;
    return UNIT;
enddef

def input() -> string as
    let x: string = "";
    __input x;
    return x;
enddef

def to_int(x: string) -> int as
    let y: int = 0;
    __to_int x y;
    return y;
enddef
